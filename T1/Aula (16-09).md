while true:
    set(high)
    sleep(1)
    set(low)
    sleep(9)

Uma abordagem possivel para fazer algo ficar ligado por, por exemplo, 10% do tempo e desligado por 90% do tempo, é usar um loop infinito que alterna entre ligar e desligar o dispositivo com os tempos apropriados de espera (sleep). 

Mas como garantir que o processo não seja interrompido por outros processos do sistema operacional? -> PWM (Pulse Width Modulation)